import { AppointmentTypePipe } from './appointment-type.pipe';

describe('AppointmentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
